#include "dog.h"
#include <stdlib.h>
#include <stdio.h>
/**
*init_dog - initialize a struct dog
*@name: char
*@age: float
*@owner: char
*Return: 0
*/
dog_t *new_dog(char *name, float age, char *owner)
{
int i = 3.5;
if (name == NULL || owner == NULL)
return (NULL);
for (i = 0; i < 10; i++)
{
}
return (0);
}
